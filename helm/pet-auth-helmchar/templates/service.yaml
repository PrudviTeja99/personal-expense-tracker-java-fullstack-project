apiVersion: v1
kind: Service
metadata:
  name: {{ include "pet-auth-helmchar.fullname" . }}
  labels:
    {{- include "pet-auth-helmchar.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pet-auth-helmchar.selectorLabels" . | nindent 4 }}
